import React from 'react';
import {Badge} from 'reactstrap';

const AddSetButton = () => {
  return (<Badge color="success">Add</Badge>);
};

AddSetButton.propTypes = {
};

export default AddSetButton;